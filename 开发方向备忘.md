# 开发备忘

> 只记录问题和大致想法，未定稿。

## 问题

1. **错题本不跨端同步**：PC 端错题本只存在浏览器本地，换浏览器 / 换手机就没了。
2. **错题本和复习口径打架**：后端复习会把错题排进复习，前端又单独维护一个本地错题本，两边对不上。
3. **文档题型清单过时**：需求文档写的是 5 种题型，实际实现是 10 种。
4. **复习跳过会误删**：跳过复习只按题号删队列，不区分题库，不同题库相同题号会被误删。

## 大概想法

* 错题本改成走后端（落 `user_data.db`），和复习共用一套数据，跨端一致。

* 顺手更新文档题型清单、修复复习误删。

***

## 安卓端改造方向

### 登录 / 账号

* 现有"登录后本地存令牌、下次直接进主页"已满足，不动。

* 账号相关设置（信息、退出登录）归并进"设置"分组，改动小。

### 产品首页（较大 UI 重构）

* 把"题库 Tab"升级成产品首页：顶部标题海报框 + 一行两个的功能宫格 + 底部统计图表（词云 / 雷达 / 折线，带动态效果）。

* 可像看文章一样向下滑动。

* 统计图表需引入 Compose 图表库（如 Vico）或自绘，这块最容易失控，建议单独一轮做。

### 刷题交互

* 滑动方向按现状：左滑 = 下一题、右滑 = 上一题（已确认不改）。

* 阅读理解：把同一篇文章的题聚合成"阅读题组"，文章固定在上，下面小题翻动。现状是文章随题重新出现并折叠，需改。

* 新增"交卷检查页"：列出全部题号，标注未答 / 已答 / 标记，点题号可跳回。现状只有半屏题号网格，没有检查页。

* 题号位置：顶部细条（题号 + 总数 + 进度），不遮挡题目，细节等真机再定。

***

## PC 端改造方向

### 刷题页左右转题符号

* 刷题页左右两侧各一个"上一题 / 下一题"浮动箭头：默认隐形，鼠标靠近显示为浅灰色，体积不大，照顾习惯差异的用户。

* 键盘 `←/→` 切题已有，`↑/↓` 待定是否绑定。

### 背景 / 主题系统（本轮核心）

* 基于现有 `tokens.css` CSS 变量体系扩展，加几套变量组即可，无需切图。

* **两类背景，严格区分（重要）**：

  * **首页背景（`day.png` / `night.png`）**：只指 HomeView 的 hero 海报区（`.hero-section` 的 `--tu-gradient-hero`）。**与主题无关**——无论切到哪套主题都不变，只按明暗模式切换：浅色态 `day.png`、暗色态 `night.png`（由 `html.dark` 统一控制）。自定义背景替换的就是这一层。

  * **主题背景**：嵌在各主题里的前端最底层全屏媒体（AppLayout 按当前 `data-theme` 注入的 `.bg-media`，如昔涟的 `cyrene.webm` 视频、天空蓝的 `∞.webp` 图片，透明度约 0.3\~0.45），**随主题变化而变化**，位于内容壳（`.app-shell`）之下。

  * 页面层级自下而上：主题背景（`.bg-media`，z-index 0）→ 页面底色/卡片（`--tu-bg` / `--tu-surface`）→ 首页 hero 海报（day/night 或自定义背景）→ 文字内容。

* 三层背景：

  * 纯白、纯黑（现在的浅蓝灰 #f4f5fa 改 #fff）

  * 自带主题 5\~6 套（莫兰迪绿 / 暖阳米 / 星空暗紫等）：配色 + 插图 + 功能框样式全用 CSS 变量 + SVG 渐变实现，每套一个 `data-theme="xxx"` 覆盖 `--tu-*`。

  * 自定义背景：**纯前端完成，不依赖后端接口**——canvas 校验分辨率（不小于屏幕）、居中裁剪为 1920×1080、逐级降质压缩（≤2.5MB），转 data URL 写入 `--tu-gradient-hero` 并存 localStorage（`toneup:ui.customBackgroundUrl`）；主题面板提供"恢复默认"。

* 主题切换 = 换 class + 存 localStorage。

### 产品首页改造

* 登录后的主页面：上半部分为背景主题海报区，往下滑动是各种功能块，每块带简短介绍，点击跳转对应功能页。

* 往下滚动有动效（滚动入场动画 / 渐变 / 视差）。

