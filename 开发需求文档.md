# 一潼上岸ToneUp（考研刷题软件）开发需求文档

**项目代号**：ToneUp
**文档版本**：V1.1
**部署环境**：阿里云 2核2G 轻量服务器 + 安卓手机 + PC浏览器

---

## 1. 项目概述

### 1.1 项目背景

开发者需构建一套跨平台（安卓原生 + PC网页）刷题系统，以目前已拥有考研数学一/二、英语一/二共4个SQLite题库文件为数据流基础，详见数据库结构文档.md。支持**持续扩充学科**、**多题型插件化渲染**、**AI拍照纠错**、**个人学习进度追踪**与**智能复习提醒**。

### 1.2 核心目标

- **舒适体验**：安卓端追求原生流畅手感（Material Design），PC端适配大屏高效操作。
- **零代码扩容**：新增学科或题库时，后端与前端核心代码**无需修改或轻量修改**，仅需配置文件调整与文件拖拽。
- **智能驱动**：接入多模态大模型，实现手写答案拍照诊断、错因分析及知识点自动归类。
- **数据主权**：题库文件（只读）与用户数据（读写）严格物理隔离，保证原始数据安全。

---

## 2. 技术选型

| 层级             | 选型                                     | 理由                                                  |
| :--------------- | :--------------------------------------- | :---------------------------------------------------- |
| **后端**   | Python 3.10 + FastAPI                    | 轻量（实际占用<150MB）、异步支持好、AI生态最成熟      |
| **数据库** | SQLite3                                  | 题库只读挂载；用户数据独立`.db`，无需独立数据库服务 |
| **PC前端** | Vue3 + Naive UI                          | 桌面级优雅组件，构建为SPA部署，浏览器直接访问         |
| **安卓端** | Kotlin + Jetpack Compose                 | 纯原生开发，性能极佳，达成最佳触控体验                |
| **AI接口** | 智谱vlm=glm-4.6v-flash/llm=glm-4.7-flash | 国内多模态模型，识别手写文字与逻辑推理能力强          |
| **部署**   | Uvicorn（ASGI）                          | 单进程模式，最大程度节省内存                          |

---

## 3. 服务器目录与数据索引设计（核心架构）

### 3.1 物理目录结构

后端采用**约定优于配置**，但不进行全盘暴力扫描，而是通过**索引文件**精确控制。

```text
/data/                          # 服务器根目录
├── manifest.json               # 【总索引】前端菜单的唯一数据源
├── user_data.db                # 【用户库】账号、进度、错题、掌握度
├── knowledge_tags.db           # 【知识点库】学科-章节-标签 树形目录
│
├── math/                       # 学科：数学（一级目录）
│   ├── zhenti/                 # 题型：真题（二级目录）
│   │   ├── math_1.db
│   │   └── math_2.db
│   ├── zhuanti/                # 题型：专项题
│   │   └── zhuan_1.db
│   └── lianxi/                 # 题型：练习题
│       └── lian_1.db
│
├── english/                    # 学科：英语
│   └── zhenti/
│       ├── english_1.db
│       └── english_2.db
│
└── [future_politics]/          # 未来新增学科（如政治）
```

### 3.2 核心索引文件（manifest.json）

后端启动时仅读取此文件（约1KB），所有前端下拉选项由此动态渲染。

```json
{
  "subjects": [
    {
      "id": "math",
      "name": "考研数学",
      "icon": "📐",
      "types": [
        { "id": "zhenti", "name": "真题", "desc": "历年套卷" },
        { "id": "zhuanti", "name": "专项题", "desc": "分模块强化" },
        { "id": "lianxi", "name": "练习题", "desc": "随机/自定义" }
      ]
    },
    {
      "id": "english",
      "name": "考研英语",
      "icon": "🔤",
      "types": [
        { "id": "zhenti", "name": "真题", "desc": "历年套卷" }
      ]
    }
  ]
}
```

**扩容操作**：新增政治时，在`/data/`下新建`politics/zhenti/`文件夹，放入`politics.db`，修改`manifest.json`加入新对象，调用重载接口即可。**后端代码零改动**。

---

## 4. 数据库设计（数据层）

### 4.1 题库文件（只读，存放于各学科目录）

为了保证**同构性**，所有`.db`文件必须包含以下核心表结构（你的数学与英语已符合）：

- **collections**：`id`, `year`, `title`
- **passages**（可选）：`id`, `collection_id`, `content`（用于英语阅读/完形）
- **questions**（核心）：`id`, `collection_id`, `question_type_id`, `number`, `content`, `options`(JSON), `answer_text`, `solution`, `score`
- **images**：`id`, `collection_id`, `question_id`, `data`(BLOB)

### 4.2 用户动态数据库（user_data.db）

独立于题库之外，存储所有可变数据。同时可以根据实际情况动态增减。

| 表名                       | 核心字段                                                                                                                                                   | 功能说明                   |
| :------------------------- | :--------------------------------------------------------------------------------------------------------------------------------------------------------- | :------------------------- |
| **users**            | `id`, `username`, `password_hash`, `created_at`                                                                                                    | 账户体系                   |
| **practice_records** | `id`, `user_id`, `db_source_path`(如 `math/zhenti/math_1.db`), `question_id`, `user_answer`, `is_correct`, `time_spent`, `created_at`    | 每一次刷题的流水账         |
| **user_mastery**     | `user_id`, `db_source_path`, `question_id`, `total_attempts`, `correct_attempts`, `last_practice_at`, `confidence_level`, `next_review_at` | 掌握程度与间隔重复算法数据 |
| **user_notes**       | `user_id`, `db_source_path`, `question_id`, `note_text`                                                                                            | 用户手动笔记               |

### 4.3 知识点标签库（knowledge_tags.db）

用于AI约束选择，可手动维护树形结构。

| 字段          | 说明                                       |
| :------------ | :----------------------------------------- |
| `id`        | 主键                                       |
| `subject`   | 对应学科ID（如`math`）                   |
| `parent_id` | 父级ID（支持层级）                         |
| `tag_name`  | 具体知识点（如“极限计算”、“虚拟语气”） |

---

## 5. 后端接口设计（RESTful API）

接口按“认证、题库、练习、复习、统计、AI、管理”划分，避免把数据库路径直接暴露为业务主键。除登录接口外均需携带 `Authorization: Bearer <token>`。响应体统一使用以下格式：

```json
{"success": true, "data": {}, "message": "", "request_id": "..."}
```

失败时 `success=false`，通过HTTP状态码区分参数错误（400）、未登录（401）、无权限（403）、资源不存在（404）和服务器错误（500）。列表接口统一支持 `page`、`page_size`，并返回 `items`、`total`、`has_more`。

### 5.1 用户认证

| 方法 | 地址 | 功能说明 | 关键入参 |
| :--- | :--- | :--- | :--- |
| **POST** | `/api/auth/register` | 注册账号 | `username`, `password` |
| **POST** | `/api/auth/login` | 登录并返回访问令牌 | `username`, `password` |
| **GET** | `/api/auth/me` | 获取当前用户信息 | 无 |

### 5.2 题库与题目

| 方法 | 地址 | 功能说明 | 关键入参 |
| :--- | :--- | :--- | :--- |
| **GET** | `/api/catalog` | 返回由 `manifest.json` 生成的学科、题型和题库目录 | 无 |
| **GET** | `/api/question-banks/{bank_id}` | 获取题库详情及可用年份/题型 | 无 |
| **GET** | `/api/question-banks/{bank_id}/questions` | 分页获取题目，可按年份、题型筛选 | `year`, `type_code`, `page`, `page_size` |
| **GET** | `/api/question-banks/{bank_id}/questions/{question_id}` | 获取题目详情、选项和解析 | 无 |

`bank_id`由后端根据索引生成并映射到真实文件路径，客户端不得自行拼接或提交任意文件路径。默认列表不返回标准答案和完整解析，提交后按权限返回，防止题库泄露。

### 5.3 练习、复习与统计

| 方法 | 地址 | 功能说明 | 关键入参 |
| :--- | :--- | :--- | :--- |
| **POST** | `/api/attempts` | 提交一次答题，记录流水并更新掌握度 | `bank_id`, `question_id`, `answer`, `time_spent`, `mode` |
| **GET** | `/api/attempts/{attempt_id}` | 获取本次答题结果及解析 | 无 |
| **GET** | `/api/reviews/today` | 获取今日待复习题目 | `limit`, `subject_id` |
| **POST** | `/api/reviews/{question_id}/skip` | 暂缓本题复习 | `next_review_at`（可选） |
| **GET** | `/api/stats/overview` | 获取正确率、刷题量、连续学习天数 | `from`, `to`, `subject_id` |
| **GET** | `/api/stats/weaknesses` | 获取按学科、题型、知识点聚合的薄弱项 | `subject_id`, `limit` |
| **GET** | `/api/questions/{question_id}/notes**` | 获取当前用户笔记 | `bank_id` |
| **PUT** | `/api/questions/{question_id}/notes` | 新增或修改当前用户笔记 | `bank_id`, `note_text` |

### 5.4 AI拍照纠错

| 方法 | 地址 | 功能说明 | 关键入参 |
| :--- | :--- | :--- | :--- |
| **POST** | `/api/ai/feedback` | 上传答案图片并创建诊断任务 | `file`, `bank_id`, `question_id`, `attempt_id`（可选） |
| **GET** | `/api/ai/feedback/{feedback_id}` | 查询诊断状态及结果 | 无 |

图片接口使用 `multipart/form-data`。小图可同步返回，大图或模型超时则返回 `202` 和 `feedback_id`，客户端轮询结果；后端自动读取题目、标准答案和知识点标签，并校验AI返回的 `tag_ids`。

### 5.5 管理接口

管理员接口使用独立权限校验：

- `POST /api/admin/catalog/reload`：重新加载并校验 `manifest.json`，成功后原子替换内存索引。
- `GET /api/admin/health`：检查索引、题库表结构、图片和解析完整性，支持 `bank_id` 筛选。
- `GET /api/admin/health/{task_id}`：查询异步健康检查结果。

所有写操作应记录用户、时间、来源IP和 `request_id`；题库查询使用只读连接，禁止客户端传入任意文件路径。

---

## 6. AI 智能纠错模块设计

### 6.1 调用流程

1. 用户拍照 -> 安卓端压缩图片 -> 上传至`/api/ai/feedback`。
2. 后端根据`db_path`和`question_id`查库，获取题干、标准答案、官方解析。
3. 后端查询`knowledge_tags.db`，获取该学科下所有标签列表。
4. 构造**严格约束Prompt**（System + User），要求AI仅能从标签列表中选择。

### 6.2 Prompt 核心策略（约束输出）

```text
[System]
你是一位考研辅导专家。请根据用户上传的答案图片、标准答案和解析，判断对错并分析。
必须从下方提供的标签列表中选择1-3个最匹配的知识点标签，输出合法JSON。
严禁捏造标签列表之外的内容。

[JSON格式要求]
{"is_correct": boolean, "error_reason": "中文原因", "tag_ids": [1, 3]}

[可用标签列表]
1. 极限计算
2. 中值定理
...
```

### 6.3 后端后处理

- 校验AI返回的`tag_ids`是否真正存在于`knowledge_tags.db`中。
- 若存在，写入`practice_records`的`ai_feedback`字段，并更新`user_mastery`中的薄弱点计数。

---

## 7. 前端（PC网页 & 安卓原生）开发要点

### 7.1 通用架构（插件化题型渲染）

两平台均采用**注册表模式**，根据`question_type_code`动态渲染UI。

- **定义标准Code**：题型定义详见 [`PC/docs/question-types.md`](PC/docs/question-types.md)（自动生成，勿手动编辑）。
- **PC端实现**：Vue3 动态组件 `<component :is="renderers[typeCode]" />`。
- **安卓端实现**：Kotlin 密封类 + 多态，根据 `typeCode` 返回不同的 `@Composable` 函数。
- **组件职责**：题型组件仅负责展示题干、选项、输入和校验状态，通过统一的 `QuestionContext` 接收题目数据、用户答案、提交状态及回调，不直接访问接口。
- **统一渲染协议**：所有题型组件至少支持 `readonly`、`disabled`、`showAnswer` 和 `showAnalysis` 状态；未知题型显示可重试的降级提示，不影响其他题目渲染。
- **组件注册**：注册表的键必须与后端 `question_type_code` 完全一致，应用启动时校验重复或缺失注册项，并输出便于定位的错误日志。
- **题型交互约定**：单选/判断立即更新当前答案，多选支持增删选项，简答/作文提供字数统计和草稿保留，完形填空按空记录答案；所有输入均支持键盘或无障碍操作。

### 7.2 数据加载流程（前端）

1. 启动 -> 请求 `/api/meta` -> 渲染左侧学科/题型树。
2. 用户点击（如“英语-真题-2025”）-> 请求 `/api/questions` -> 获得题目列表。
3. 切换题目 -> 前端仅切换显示，不重复请求后端（题目数据已缓存于前端内存）。

补充要求：

- **请求状态**：接口调用必须具备加载中、空数据、失败和重试状态；切换题目时保留已加载内容，避免页面闪烁。
- **缓存策略**：以“学科、题型、年份、筛选条件”为缓存键，缓存题目列表和元数据；支持手动刷新，并在切换账号或退出登录时清理用户相关缓存。
- **分页与预加载**：题目数量较多时采用分页或游标加载，当前题目加载完成后可预加载相邻题目；不得一次性加载超出当前练习范围的大量图片资源。
- **数据一致性**：提交成功后以服务端返回结果为准更新本地记录；网络中断时保留用户答案并提示重试，避免重复提交。

### 7.3 刷题核心交互（双方需对齐）

- **答题模式**：点击选项/输入框 -> 点击“确认答案” -> 前端请求`/api/practice/submit` -> 后端返回正确/错误及解析 -> 前端展示解析并自动记录。
- **复习模式**：请求`/api/review/today` -> 仅展示待复习题目 -> 交互流程同上。

### 7.4 页面与状态设计

- **设计目标**：刷题界面以零干扰、即时反馈和平台差异化为原则；无关元素自动隐退，核心操作保持单手或键盘可达。
- **页面结构**：PC端采用“左导航 + 右主区”布局，左侧导航宽度 240px 且可折叠，主区包含题目、解析及进度/知识点信息；窄屏时右侧信息折叠为抽屉。安卓端采用底部导航切换“题库、复习、统计、我的”。
- **页面流程**：首页提供学习计划、连续打卡、继续上次刷题和学科入口；题库选择页支持“学科 -> 题型 -> 年份/章节”三级联动，PC使用面包屑和文件夹树，安卓使用半屏 Bottom Sheet。
- **练习状态**：统一维护 `loading`、`idle`、`editing`、`submitting`、`submitted`、`error` 状态，提交期间禁用重复点击；加载时显示骨架屏，切换题目保留已加载内容，失败提供重试。
- **答题进度**：展示当前题号、已答数量、完成比例及进度环；切换题目、刷新或返回页面前自动保存未提交草稿，并在恢复时提示用户。
- **导航行为**：支持上一题/下一题、题号面板、未答题筛选和标记重点复习；离开未提交内容时弹出确认提示。PC支持 `A/B/C/D` 选答案、`Enter` 确认/下一题、`←/→` 翻页及 `Space` 长按标记疑问，并显示快捷键提示。
- **安卓交互**：刷题时支持沉浸模式、边缘左右滑动切题、下拉刷新；底部操作栏距屏幕底部手势区至少 80dp，核心按钮点击区域不小于 48dp。
- **数据策略**：进入刷题页时可一次性预加载当前试卷题目 JSON 并存入内存；答案实时写入 Web `LocalStorage` 或安卓 `DataStore`，断网可继续切题，联网后自动同步并以服务端结果为准。

### 7.5 视觉、可用性与适配

- **设计系统**：主色为深静谧蓝 `#2B3A67`，强调色为活力紫 `#7C3AED`。PC浅色模式使用轻量毛玻璃侧栏，暗色模式使用深空灰渐变；安卓跟随 Material You 动态颜色。PC字体使用 Inter/PingFang SC，安卓使用 Roboto/Noto Sans SC，正文行高为 1.75。
- **反馈规范**：正确、错误、待提交和已作答状态使用文字、图标与颜色共同表达；选项点击提供 Ripple/光晕反馈，错误提供轻微 3° 晃动及就近修正建议，解析可平滑展开并明确区分用户答案和正确答案。
- **平台特效**：PC选项悬停上浮约 2px 并显示光晕，解析页支持题干与解析分栏；安卓支持 Collapsing Toolbar、共享轴转场和轻量触感反馈（收藏 LIGHT_IMPACT、提交错误 WARNING、连续打卡 SUCCESS）。动效默认 150–300ms，并支持关闭动效或低性能模式。
- **响应式适配**：PC支持 1920×1080 和 1366×768；安卓适配 320dp 起的宽度、横竖屏、系统字体缩放和深色模式。长题干、公式、图片及解析区域可滚动且不裁切，选项在PC横排、安卓竖排。
- **无障碍**：交互控件提供语义化名称、焦点顺序、键盘操作、屏幕阅读器支持和触摸反馈；最小点击区域不小于 44dp，反馈不依赖颜色。
- **性能要求**：长列表使用虚拟滚动，图片按需加载并显示占位图，禁止预加载超出练习范围的大量图片；避免切题时重复创建组件，普通设备上切题和提交保持流畅，目标帧率 60fps。
- **验收标准**：连续刷题 1 小时核心操作均在单手可达区域；页面无横向溢出或错位，动画和转场不卡顿；加载、空数据、失败、重试及断网草稿恢复流程均可验证。

### 7.6 前端接口与错误处理

- API 请求统一封装鉴权、超时、取消请求和 JSON 错误解析；认证失效时跳转登录并保留当前页面恢复信息。
- 错误按网络异常、参数错误、权限不足和服务端异常分类展示，面向用户使用中文提示，同时在开发环境记录请求标识和详细日志。
- `/api/practice/submit` 请求携带题目ID、答案、模式和客户端请求ID，前端使用请求ID防止重复提交；提交结果需更新本地练习进度和解析展示。
- 前端不信任本地答案或正确性字段，正确答案、解析、掌握度及复习日期均以服务端返回数据为准。

---

## 8. 非功能性需求（NFR）

- **内存优化**：后端使用`functools.lru_cache`管理SQLite连接，最大缓存5个，闲置释放，确保占用<200MB。
- **扩展性**：新增题型时，只需在前端两个工程（PC+安卓）新增组件文件并注册，后端无需改动。
- **数据安全**：题库文件挂载为`PRAGMA query_only = ON`，杜绝误删误改。
- **备份机制**：每日凌晨定时压缩`/data/`目录，保留最近7天备份。

---

## 9. V1.1 设计修订与实现约束

### 9.1 统一接口协议

以下接口名称为正式协议：`GET /api/catalog`、`GET /api/question-banks/{bank_id}/questions`、`POST /api/attempts`、`GET /api/reviews/today`、`GET/PUT /api/questions/{question_id}/notes`。其他文档和客户端不得再使用同义旧路径。

所有响应使用统一外层结构，列表数据放在`data.items`中，并包含`total`和`has_more`。接口契约以FastAPI生成的OpenAPI文档为准，前端类型由该契约生成。

### 9.2 题库身份与路径映射

客户端只能使用`bank_id`，不得提交或拼接服务器文件路径。`manifest.json`中的题库条目必须包含：`id`、`subject_id`、`type_id`、`name`、`path`、`schema_version`和`enabled`。

服务端启动和重载时必须校验ID唯一、路径位于数据根目录内、文件存在、SQLite表结构符合版本要求。业务代码不得假设题库目录层级。

### 9.3 统一题目 DTO 与题型映射

英语和数学数据库允许存在结构差异，后端Repository负责转换为统一DTO，客户端不得直接依赖SQLite字段。统一题目至少包含：`bank_id`、`question_id`、`collection_id`、`year`、`content`、`passage`、`options`、`sub_questions`、`type_code`和`display_order`。

`question_type_id`必须通过配置映射为`SINGLE`、`MULTI`、`JUDGE`、`ESSAY`、`CLOZE`等稳定的`type_code`，不得在两个客户端分别硬编码。

### 9.4 答题幂等与数据一致性

`POST /api/attempts`必须携带客户端生成的`client_request_id`。服务端对`user_id + client_request_id`建立唯一约束；重复提交时返回第一次结果，不重复增加记录或掌握度。

断网时客户端只保存未提交草稿；正确性、解析、掌握度和复习时间始终以服务端返回为准。切换账号或退出登录时清理用户相关缓存。

### 9.5 AI任务与上传约束

AI反馈状态固定为`queued`、`processing`、`succeeded`、`failed`，任务结果和失败原因必须持久化，避免服务重启后任务永久丢失。

上传接口必须限制文件类型、大小、图片尺寸和频率。AI输出经过JSON Schema校验，并验证`tag_ids`属于当前学科；模型返回内容不得直接作为数据库查询条件或HTML渲染内容。

### 9.6 认证、部署与备份

- 密码使用Argon2或bcrypt保存，禁止明文和自定义哈希。
- 访问令牌必须有过期时间，登录、注册和AI接口必须限流。
- 生产环境使用HTTPS和反向代理，CORS仅允许配置的前端来源。
- 题库连接使用只读模式，用户库使用事务和WAL模式。
- 备份包含用户隐私，必须限制文件权限并加密或存放在受控位置，且定期演练恢复流程。

### 9.7 MVP分期

首版只实现`SINGLE`、`MULTI`和`JUDGE`，完成登录、题库浏览、答题提交、历史记录和基础统计。第二阶段加入错题与间隔复习，第三阶段加入安卓端，第四阶段再加入AI拍照纠错、简答题、完形填空和管理后台。

首版复习间隔采用简单规则：答对按`1、3、7、14、30`天递增，答错安排当天稍后或次日复习；后续可替换为FSRS，但接口字段保持兼容。

---
